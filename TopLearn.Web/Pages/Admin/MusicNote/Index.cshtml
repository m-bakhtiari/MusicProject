@page
@model TopLearn.Web.Pages.Admin.MusicNote.IndexModel
@{
    ViewData["Title"] = "لیست نت ها";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">لیست نت ها</h1>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست نت ها
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        <div class="col-md-12" style="margin: 10px 0;">

                            <a asp-page="CreateMusicNote" class="btn btn-outline btn-success">افزودن نت جدید</a>

                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr>
                                    <th>عنوان نت</th>
                                    <th>#</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.MusicNotes)
                                {
                                    <tr>
                                        <td>@item.Title</td>
                                        <td>
                                            <a href="/Admin/MusicNote/DeleteMusicNote/@item.MusicNoteId" class="btn btn-info btn-sm">
                                                حذف
                                            </a>
                                            <a href="/Admin/MusicNote/EditMusicNote/@item.MusicNoteId" class="btn btn-warning btn-sm">
                                                ویرایش
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
