@page
@model TopLearn.Web.Pages.Admin.Student.IndexModel
@{
    ViewData["Title"] = "لیست";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">لیست </h1>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        <div class="col-md-12" style="margin: 10px 0;">

                            <a asp-page="CreateStudent" asp-route-type="@ViewData["type"]" class="btn btn-outline btn-success">افزودن</a>
                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>نام و نام خانوادگی</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.Students)
                                {
                                    <tr>
                                        <td>
                                            <a href="/Admin/Student/DeleteStudent/@item.StudentId" class="btn btn-info btn-sm">
                                                حذف
                                            </a>
                                            <a href="/Admin/Student/EditStudent/@item.StudentId" class="btn btn-warning btn-sm">
                                                ویرایش
                                            </a>
                                        </td>
                                        <td>@item.StudentFullName</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
